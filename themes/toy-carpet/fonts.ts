// Toy Carpet theme fonts
// Uses system default for a clean, playful look
// Could be replaced with a rounded/comic font like "Comic Neue" in the future

export const fonts = {
  display: 'System',  // Falls back to system font
};

// No custom font assets needed - uses system font
export const fontAssets = {};
